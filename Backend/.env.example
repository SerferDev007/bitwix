# Server
PORT=5000
# Comma-separated list of allowed frontend origins for CORS
CORS_ORIGIN=http://localhost:5173

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bitwix
